#ifndef SPIC_FOLDERFILE_HPP
#define SPIC_FOLDERFILE_HPP

#include <string>

namespace spic
{
    /**
     * @brief A models for storing file information.
     */
    struct FolderFile
    {
        std::string Name;
        std::string File;
    };
}

#endif //SPIC_FOLDERFILE_HPP
